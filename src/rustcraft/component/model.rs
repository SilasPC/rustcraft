
use crate::vao::VAO;
use std::sync::Arc;
use cgmath::*;
use rand::prelude::*;
use super::*;

pub struct Model {
    pub model: Arc<VAO>,
}

impl Model {
    pub fn system_render(data: &mut crate::Data) {
        
    }
}